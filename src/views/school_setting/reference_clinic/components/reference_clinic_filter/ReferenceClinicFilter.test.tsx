import * as React from 'react'
import { render } from '@testing-library/react'
import ReferenceClinicFilter from './ReferenceClinicFilter'

test('renders without crash', () => {
  render(<ReferenceClinicFilter />)
})
