import * as React from 'react'
import { render } from '@testing-library/react'
import SicepatPickupFilter from './SicepatPickupFilter'

test('renders without crash', () => {
  render(<SicepatPickupFilter />)
})
