import { IReferenceServiceReport } from '../../../interfaces'

export declare interface IReferenceServiceReportDatagridProps {
  loading: boolean
  rows: IReferenceServiceReport[]
  // onExport?: () => void
}
